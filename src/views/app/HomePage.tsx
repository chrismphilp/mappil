import LandingPageShell from '../../components/landing/LandingPageShell';
import { HOME_PAGE_CONTENT } from '../../lib/landingContent';

const HomePage = () => {
  return <LandingPageShell content={HOME_PAGE_CONTENT} />;
};

export default HomePage;
